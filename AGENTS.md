# MinuteMark 프로젝트 작업 규칙

## 버전업 기록

- 버전업을 시작하기 전에 `docs/` 아래에 해당 버전의 설계·출시 문서를 만들거나,
  이미 활성 문서가 있으면 그 문서를 정본으로 갱신한다.
- 문서에는 범위와 비범위, 주요 설계 결정, 업데이트·버그 수정 내역, 데이터와
  보안 영향, 테스트 결과, 배포·복구 방법, 남은 문제를 기록한다.
- 같은 버전의 상태 문서를 여러 개 만들지 않는다. README에서 현재 정본 문서를
  링크하고, 계획부터 실제 검증·배포 결과까지 한 문서에서 이어 관리한다.
- PR을 병합하기 전에 Spec 리뷰와 Standards/보안 리뷰를 각각 한 번 수행한다.
  출시를 막는 항목은 수정하고 관련 테스트를 다시 실행한다.
- 필수 출시 게이트가 통과한 커밋만 `main`에 병합하고 배포한다. 통과하지 못한
  항목은 완료로 표현하지 않고 `FAIL` 또는 `BLOCKED`와 다음 행동을 남긴다.
